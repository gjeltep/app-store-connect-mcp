# App Store Connect credentials (used by the MCP server)

# Required: The Key ID of your App Store Connect API key (found in Users and Access → Keys).
APP_STORE_KEY_ID=

# Required: The Issuer ID (a UUID) associated with your App Store Connect API key.
APP_STORE_ISSUER_ID=

# Required: Absolute path to your private key file (.p8), e.g., /Users/me/AuthKey_XXXXXX.p8
APP_STORE_PRIVATE_KEY_PATH=

# Optional: The App resource ID (e.g., 1234567890). If not set, some tools require app_id input.
APP_STORE_APP_ID=

# Optional: Key type - "team" (default) or "individual"
# APP_STORE_KEY_TYPE=team

# Optional: Comma-separated scopes for individual keys (e.g., "GET /v1/apps,GET /v1/builds")
# APP_STORE_SCOPE=

# Optional: Subject claim for individual keys (defaults to "user" if not set)
# APP_STORE_SUBJECT=


# Model generation inputs (used by scripts/generate_models.py)
#   Defaults to fetching from public endpoint, but as alternative:
#   Absolute path to a local App Store Connect OpenAPI JSON spec
# APP_STORE_CONNECT_OPENAPI_PATH=/absolute/path/to/app_store_connect_api_openapi.json
